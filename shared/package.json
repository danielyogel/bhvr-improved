{
  "name": "shared",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --pretty",
    "type-check": "tsc --noEmit --pretty"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
